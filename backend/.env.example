# Database Configuration
# PostgreSQL connection string
DATABASE_URL=postgresql://metabase:BioMass@786@109.199.116.191:5432/DigitalNurse

# JWT Configuration
# Secret key for signing JWT tokens (generate a strong random string)
JWT_SECRET="your-jwt-secret-key-change-this-in-production"
# Access token expiration time (e.g., "15m", "1h", "7d")
JWT_EXPIRATION="7d"
# Secret key for signing refresh tokens (should be different from JWT_SECRET)
JWT_REFRESH_SECRET="your-jwt-refresh-secret-key-change-this-in-production"
# Refresh token expiration time (e.g., "30d", "90d")
JWT_REFRESH_EXPIRATION="30d"

# Google OAuth Configuration (Optional - App will work without these)
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a project or select existing one
# 2. Enable Google+ API
# 3. Go to "Credentials" > "Create Credentials" > "OAuth client ID"
# 4. Choose "Web application"
# 5. Add authorized redirect URI: http://localhost:3000/api/auth/google/callback
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL="http://localhost:3000/api/auth/google/callback"

# Stripe Configuration (Optional - App will work without these)
# Get these from Stripe Dashboard: https://dashboard.stripe.com/
STRIPE_SECRET_KEY=""
# Webhook signing secret (get from Stripe CLI: stripe listen --forward-to localhost:3000/api/subscriptions/webhooks/stripe)
STRIPE_WEBHOOK_SECRET=""
# Stripe Price IDs for subscription plans (create products/prices in Stripe Dashboard)
STRIPE_BASIC_PRICE_ID=""
STRIPE_PREMIUM_PRICE_ID=""

# Application Configuration
# Frontend URL for CORS and OAuth redirects
FRONTEND_URL="http://localhost:3000"
# Server port (defaults to 3000 if not set)
PORT=3000

# Node Environment
# Set to "production" in production
NODE_ENV="development"
