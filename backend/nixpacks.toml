[phases.install]
cmds = ["npm config set cache /tmp/.npm", "rm -rf /app/node_modules/.cache || true", "npm ci"]

[phases.build]
cmds = ["npm run build", "npx prisma generate"]

[start]
cmd = "npm run start:prod"

